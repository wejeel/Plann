<%= form_with(model: trip_todo, local: true) do |form| %>
  <% if trip_todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_todo.errors.count, "error") %> prohibited this trip_todo from being saved:</h2>

      <ul>
      <% trip_todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
