<%= form_with(model: suggestions_box_trip, local: true) do |form| %>
  <% if suggestions_box_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestions_box_trip.errors.count, "error") %> prohibited this suggestions_box_trip from being saved:</h2>

      <ul>
      <% suggestions_box_trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
